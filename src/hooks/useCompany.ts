// Delega al contexto compartido — un solo fetch por sesión para toda la app
export { useCompanyContext as useCompany } from '@/context/CompanyContext';
export type { CompanyData } from '@/context/CompanyContext';
